[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "shl-assessment-recommender"
version = "1.0.0"
description = "SHL Conversational Assessment Recommender — RAG-powered REST API"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
